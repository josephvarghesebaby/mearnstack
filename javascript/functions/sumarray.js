var arr=[2,6,8]
var sum=0;
 var res=0;
    
for (let i of arr){
    sum=(arr[0]+arr[1]+arr[2])
   a=sum+arr[0];
   b=sum+arr[1];
   c=sum+arr[2];
}
       
console.log(sum,a,b,c);
 
//op.push((sum-num))    // for printing sum + elements